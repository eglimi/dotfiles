[general]
# Save button writes a timestamped file here; copy button writes to the clipboard.
output-filename = "~/Pictures/Screenshots/satty-%Y%m%d-%H%M%S.png"
copy-command = "wl-copy"
early-exit = true
